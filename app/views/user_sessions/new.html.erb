<%= form_with url: login_path, local: true do |f| %>
	<%= f.label :email, "メールアドレス" %>
	<%= f.text_field :email %>

	<%= f.label :password, "パスワード" %>
	<%= f.password_field :password %>

	<%= f.submit 'ログイン' %>
<% end %>

<%= link_to 'パスワードを忘れました', new_password_reset_path %>